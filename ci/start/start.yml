---
platform: linux
image_resource:
    type: docker-image
    source: {repository: ranger6/alpine-extras, tag: "3.13"}
inputs:
    - name: main
    - name: version
outputs:
    - name: release
    - name: release-branch
run:
    path: main/ci/start/start.sh
    args:
    - "version/version"
    - "((.:release-manifest.branch-hash))"
    - "((.:release-manifest.release-branch-prefix))"
